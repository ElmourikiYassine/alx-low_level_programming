#include<stdio.h>
/**
 * main - This is the entry
 * Return: - if everything alright
 */
int main(void)
{
	char sen[200] = "\"Programming is like building a multilingual puzzle";

	puts(sen);
	return (0);
}
