#include<stdio.h>
/**
* main - this is the entry
* Return: 1 if no error accured
*/
int main(void)
{
	char sen[200] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", sen);
	return (0);
}
